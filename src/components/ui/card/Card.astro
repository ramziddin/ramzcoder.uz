---
import { twMerge } from "tailwind-merge";

const { class: className } = Astro.props;
---

<div
  class={twMerge(
    "w-full max-w-md bg-zinc-900 border border-zinc-800 rounded-lg overflow-hidden",
    className
  )}
>
  <slot />
</div>
